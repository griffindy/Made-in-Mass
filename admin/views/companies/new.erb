<div class="block">
  <div class="secondary-navigation">
    <ul class="wat-cf">
      <li class="first"><%= link_to pat(:list), url(:companies, :index) %></li>
      <li class="active"><%= link_to pat(:new), url(:companies, :new) %></li>
    </ul>
  </div>
  <div class="content">
    <h2 class="title"><%= pat(:new) %> <% mt(:company) %></h2>
    <div class="inner">
      <% form_for :company, url(:companies, :create), :class => :form do |f| %>
        <%= partial "companies/form", :locals => { :f => f } %>
      <% end %>
    </div>
  </div>
</div>
<% content_for :sidebar, partial("base/sidebar") %>
